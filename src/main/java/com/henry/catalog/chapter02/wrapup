概念：
1 行为参数化 - 一个方法，能够接受不同的行为作为参数，并在方法内部使用接收到的行为。以完成不同的操作
2 行为参数化能够使得代码更好地适应变化的需求，从而减轻未来的工作量；

3 传递行为到方法的实现手段：
    手段1：一个接口类型 + N个实现类（用于封装具体的行为）；
    优化1：不需要准备实现类，而是 使用匿名类在声明时直接实例化；
    优化2：在Java8之后，可以使用lambda表达式；
4 Java API中提供了很多 可以接收参数化行为的方法：排序、线程、GUI处理。